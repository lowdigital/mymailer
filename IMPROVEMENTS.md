# MyMailer v2.1 - Список улучшений

Эта версия включает значительные функциональные и интерфейсные доработки, превращающие MyMailer в мощный инструмент для персонализированных рассылок.

## 🚀 Новые функции

### 1. Персонализация через CSV (Расширенный режим)
- Добавлена поддержка динамических переменных. Теперь можно загружать списки в формате CSV (первая строка — заголовки).
- Любой заголовок (например, `name`, `price`, `product`) можно использовать в теле письма в фигурных скобках: `{name}`, `{price}`.
- Система автоматически сопоставляет данные для каждого получателя.

### 2. Умное управление списком получателей
- **Импорт CSV:** Добавлена возможность загрузки файлов `.csv` и `.txt` напрямую в браузер с автоматическим распознаванием формата.
- **Улучшенный счетчик:** Счетчик получателей теперь игнорирует строку заголовка и пустые строки, показывая реальное количество писем к отправке.

### 3. Автоматизация сохранения
- **Auto-save на вкладках:** При переключении между настройками, списком получателей и шаблоном данные сохраняются автоматически.
- **Безопасный запуск:** При нажатии кнопки «Запустить рассылку» система принудительно сохраняет текущую вкладку перед переходом к отправке, исключая потерю данных.

### 4. Система архивации
- Кампании теперь можно отправлять в **Архив**, чтобы они не загромождали главную страницу.
- Интерфейс разделен на вкладки «Активные» и «Архив».
- Добавлена возможность быстрого восстановления рассылки из архива.

## 🎨 Интерфейс и UX
- Обновлены шаблоны писем по умолчанию на более современные и адаптивные.
- Добавлены визуальные подсказки (тосты) при импорте и сохранении.
- Индикатор загрузки (Loading Overlay) при выполнении критических операций.

## 🛠 Технические изменения
- Оптимизирован парсинг CSV в `config.php`.
- Исправлена логика в `admin/send.php` для поддержки динамических замен.
- Обновлены настройки SMTP для легкой интеграции с Gmail и другими современными сервисами.

---
*Разработано с целью сделать email-маркетинг более простым и эффективным.*